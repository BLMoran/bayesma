# Check if RoBMA Sensitivity Fits are Available

Checks whether a bayesma object has pre-computed RoBMA sensitivity fits
attached.

## Usage

``` r
has_robma_sensitivity(model)
```

## Arguments

- model:

  A `bayesma` object.

## Value

Logical. TRUE if robma_sensitivity is attached, FALSE otherwise.
