# Print Stan code from a fitted model

Print Stan code from a fitted model

## Usage

``` r
stan_code(fit)
```

## Arguments

- fit:

  A `CmdStanFit` object.

## Value

The Stan code string (invisibly).
