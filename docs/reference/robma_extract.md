# Extract tidy effect components from a RoBMA fit

Extract tidy effect components from a RoBMA fit

## Usage

``` r
robma_extract(fit, spec)
```

## Arguments

- fit:

  A `bayesma_robma_fit`.

- spec:

  A `bayesma_robma_spec`.

## Value

An object of class `"bayesma_robma_effects"`.
