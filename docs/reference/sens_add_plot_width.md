# Update the relative width of the density plot

Update the relative width of the density plot

## Usage

``` r
sens_add_plot_width(x, plot_width)
```

## Arguments

- x:

  A `bayesma_sensitivity_plot` object.

- plot_width:

  Numeric. Relative width of the density plot panel.

## Value

Modified `bayesma_sensitivity_plot`.
