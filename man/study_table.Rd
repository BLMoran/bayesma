% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_table.R
\name{study_table}
\alias{study_table}
\title{Create a Table of Included Studies}
\usage{
study_table(
  data,
  rob_tool = c("auto", "none", "rob2", "robins_i", "newcastle_ottawa", "quadas2"),
  outcome_type = c("auto", "continuous", "binary"),
  subgroup_col = NULL,
  font = NULL
)
}
\arguments{
\item{data}{A data frame or tibble with one row per study. Expected columns
depend on \code{outcome_type} and \code{rob_tool} — see the bundled templates
(\code{\link[=download_template]{download_template()}}) for the exact layout.}

\item{rob_tool}{Risk-of-bias tool. One of \code{"auto"} (detect from column
names), \code{"none"}, \code{"rob2"}, \code{"robins_i"}, \code{"newcastle_ottawa"}, or
\code{"quadas2"}.}

\item{outcome_type}{Whether outcomes are \code{"continuous"}, \code{"binary"}, or
\code{"auto"} (detect from column names).}

\item{subgroup_col}{Unquoted name of the subgroup column, or \code{NULL} (no
grouping).}

\item{font}{Font family for the table. \code{NULL} uses the gt default.}
}
\value{
A \code{\link[gt:gt]{gt::gt()}} table object.
}
\description{
Produces a formatted \code{\link[gt:gt]{gt::gt()}} table from a study-level data frame. Handles
continuous and binary outcomes, subgroup grouping, and traffic-light
risk-of-bias formatting for RoB 2, ROBINS-I, NOS, and QUADAS-2.
}
\examples{
\dontrun{
data(binary_outcome)
study_table(binary_outcome)

data(cont_outcome)
study_table(cont_outcome)
}

}
