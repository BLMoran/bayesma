% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesma_report.R
\name{bayesma_report}
\alias{bayesma_report}
\title{Generate a Bayesian meta-analysis report}
\usage{
bayesma_report(
  data,
  studyvar,
  yearvar,
  estimand = c("OR", "RR", "HR", "IRR", "MD", "SMD", "RD", "ARR", "ATE", "ATT", "CATE"),
  cate_covariate = NULL,
  baseline_risk = NULL,
  event_ctrl = NULL,
  event_int = NULL,
  mean_ctrl = NULL,
  mean_int = NULL,
  sd_ctrl = NULL,
  sd_int = NULL,
  n_ctrl = NULL,
  n_int = NULL,
  outcome_name = "the primary outcome",
  null_range,
  title = "Bayesian meta-analysis report",
  subtitle = "Generated with bayesma",
  author = "",
  prospero_id = "",
  background = "",
  hypothesis = "",
  inclusion_criteria = character(),
  exclusion_criteria = character(),
  quality_tool = "Cochrane RoB 2",
  software_note = "",
  rob_data = NULL,
  priors = NULL,
  run_inspect_sr = FALSE,
  run_rob = FALSE,
  run_egger = TRUE,
  run_robma = TRUE,
  run_metareg = FALSE,
  run_subgroup = FALSE,
  run_sensitivity_priors = TRUE,
  moderators = character(),
  subgroup_var = character(),
  primary_model = "auto",
  re_min_k = NULL,
  output_file,
  render = FALSE
)
}
\arguments{
\item{data}{A data frame with one row per study (or per arm).}

\item{studyvar}{Character. Column name with study identifiers.}

\item{yearvar}{Character. Column name with publication year.}

\item{estimand}{Character. Target effect: \code{"OR"}, \code{"RR"}, \code{"HR"}, \code{"IRR"},
\code{"MD"}, \code{"SMD"}, \code{"RD"} / \code{"ARR"}, \code{"ATE"}, \code{"ATT"}, or \code{"CATE"}. The
marginal estimands (RD/ATE/ATT/CATE) are computed via \code{\link[=bayesma_marginal]{bayesma_marginal()}}
on the natural scale.}

\item{cate_covariate}{Character. Study-level covariate name. Required when
\code{estimand = "CATE"}.}

\item{baseline_risk}{Numeric in (0, 1) or \code{"study_mean"}. Reference baseline
used to back-transform two-stage binomial fits to RD/ATE/ATT.}

\item{event_ctrl, event_int, n_ctrl, n_int}{Character. Column names for event
counts and arm sizes (binary measures).}

\item{mean_ctrl, mean_int, sd_ctrl, sd_int}{Character. Column names for arm
means and SDs (continuous measures).}

\item{outcome_name}{Character. Human-readable outcome label used in titles
and prose.}

\item{null_range}{Numeric length-2 vector. Null/ROPE range on the natural
scale (e.g. \code{c(0.9, 1.1)} for an OR).}

\item{title, subtitle, author}{Character. Report metadata.}

\item{prospero_id}{Character. Optional PROSPERO registration identifier.}

\item{background, hypothesis}{Character (scalar or vector). Prose for the
Introduction and Methods sections. Each element becomes a paragraph.}

\item{inclusion_criteria, exclusion_criteria}{Character vectors. Each element
becomes a bullet point.}

\item{quality_tool}{Character. Risk-of-bias tool name (default Cochrane RoB 2).}

\item{software_note}{Character. Optional override for the Software paragraph.}

\item{rob_data}{Optional pre-scored risk-of-bias data frame for \code{\link[=rob_plot]{rob_plot()}}.
Used when \code{run_rob = TRUE}.}

\item{priors}{Optional named list of prior settings for the sensitivity
panel. Each element is a list with \code{mu_prior} and \code{tau_prior}. Defaults
to \code{vague} / \code{weak_reg} / \code{informative}.}

\item{run_inspect_sr, run_rob, run_egger, run_robma, run_metareg, run_subgroup, run_sensitivity_priors}{Logical. Section toggles.}

\item{moderators}{Character vector. Moderator columns for meta-regression.
Required when \code{run_metareg = TRUE}.}

\item{subgroup_var}{Character vector. Subgroup column(s). Required when
\code{run_subgroup = TRUE}.}

\item{primary_model}{Character. \code{"auto"} (default; selected via
\code{\link[=compare_models]{compare_models()}}) or one of the candidate model labels (e.g.
\code{"two_stage_re"}, \code{"two_stage_re_hksj"}).}

\item{re_min_k}{Optional numeric. Forwarded to \code{\link[=bayesma]{bayesma()}} (and used per
subgroup level): downgrade random-effects to common-effect when the
number of studies in scope is below this threshold. Default \code{NULL}.}

\item{output_file}{Character. Path for the generated \code{.qmd}.}

\item{render}{Logical. If \code{TRUE}, also render the \code{.qmd} to HTML.}
}
\value{
Invisibly, the path to the generated \code{.qmd} file.
}
\description{
Renders a parameterised Quarto template into a publication-style statistical
report. Section structure follows the bayesma workflow guide; prose style
follows a typical systematic review (Introduction, Methods, Results, Overall
Interpretation). Most sections are toggleable via \verb{run_*} arguments.
}
\details{
The function writes a \code{.qmd} file plus a companion \code{.rds} (containing
\code{data}, \code{rob_data}, and \code{priors}) to the directory of \code{output_file}. With
\code{render = TRUE} it then renders the \code{.qmd} to HTML via the Quarto CLI.
}
\examples{
\dontrun{
bayesma_report(
  data         = eeg_data,
  studyvar     = "Author",
  yearvar      = "Year",
  estimand     = "OR",
  event_ctrl   = "c_event", event_int = "i_event",
  n_ctrl       = "c_n",     n_int     = "i_n",
  outcome_name = "Postoperative Delirium",
  null_range   = c(0.9, 1.1),
  title        = "EEG-guided anaesthesia and POD",
  author       = "Dr Benjamin Moran",
  output_file  = "eeg_pod_report.qmd",
  render       = TRUE
)
}

}
