% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/egger.R
\name{egger}
\alias{egger}
\title{Egger's Regression Test for Small-Study Effects (Bayesian)}
\usage{
egger(
  data,
  studyvar,
  n_ctrl,
  n_int,
  event_ctrl = NULL,
  event_int = NULL,
  mean_ctrl = NULL,
  mean_int = NULL,
  sd_ctrl = NULL,
  sd_int = NULL,
  likelihood = c("binomial", "gaussian", "poisson"),
  heterogeneity = c("multiplicative", "additive"),
  alpha_prior = NULL,
  beta_prior = NULL,
  kappa_prior = NULL,
  gamma_prior = NULL,
  d_prior = NULL,
  tau_prior = NULL,
  credible_level = 0.9,
  return_stage = c("full", "spec", "code", "data", "fit"),
  chains = 4,
  iter_warmup = 2000,
  iter_sampling = 4000,
  adapt_delta = 0.95,
  seed = 1234,
  custom_model = NULL,
  custom_data = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame with one row per study.}

\item{studyvar}{Character. Column name of the study identifier.}

\item{n_ctrl, n_int}{Character. Column names of control and intervention
sample sizes.}

\item{event_ctrl, event_int}{Character. Column names of event counts
(binomial / Poisson likelihoods).}

\item{mean_ctrl, mean_int, sd_ctrl, sd_int}{Character. Column names of arm
means and SDs (Gaussian likelihood).}

\item{likelihood}{Character. One of \code{"binomial"}, \code{"gaussian"},
\code{"poisson"}.}

\item{heterogeneity}{Character. \code{"multiplicative"} (default) or
\code{"additive"}.}

\item{alpha_prior}{Prior on the intercept.}

\item{beta_prior}{Prior on the slope (the Egger coefficient).}

\item{kappa_prior}{Prior on the multiplicative heterogeneity
coefficient.}

\item{gamma_prior}{Prior on the dispersion parameter.}

\item{d_prior}{Prior on the overdispersion parameter.}

\item{tau_prior}{Prior on the between-study SD for the additive
heterogeneity parameterisation.}

\item{credible_level}{Numeric in \verb{(0, 1)}. Credible-interval level for
the summary. Default \code{0.90}.}

\item{return_stage}{Character. One of \code{"full"} (default), \code{"spec"},
\code{"code"}, \code{"data"}, or \code{"fit"}.}

\item{chains, iter_warmup, iter_sampling, adapt_delta, seed}{MCMC settings.}

\item{custom_model}{Optional character scalar of Stan code overriding
the generated program.}

\item{custom_data}{Optional named list merged into the Stan data list.}

\item{...}{Passed to \code{cmdstanr::sample()}.}
}
\value{
An object of class \code{c("bayesma_egger", "bayesma")}.
}
\description{
Thin orchestrator over the six-stage pipeline:
\code{\link[=egger_spec]{egger_spec()}}, \code{\link[=egger_stan_code]{egger_stan_code()}}, \code{\link[=egger_stan_data]{egger_stan_data()}}, \code{\link[=egger_fit]{egger_fit()}},
\code{\link[=egger_extract]{egger_extract()}}, \code{\link[=egger_output]{egger_output()}}.
}
