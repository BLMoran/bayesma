% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robma_pipeline.R
\name{robma_stan_code}
\alias{robma_stan_code}
\title{Generate Stan programs for a RoBMA specification}
\usage{
robma_stan_code(spec, format = TRUE)
}
\arguments{
\item{spec}{A \code{bayesma_robma_spec}.}

\item{format}{Logical. Run each program through \code{stanc --auto-format}.}
}
\value{
An object of class \code{"bayesma_robma_stan_code"}.
}
\description{
For \code{method = "bridge"} returns a named list keyed by model label. Each entry
is either a \code{bayesma_stan_code}-shaped list (parsed blocks + \code{full}) or, for
analytic component models with no Stan program, a list with \code{analytic = TRUE}
and \code{full = NA_character_}.
}
\details{
For \code{method = "ss"} returns a single-entry list keyed by \code{bias_indicator}.

If \code{spec$custom_model} is set, the user's Stan program(s) replace the
generated code: a character scalar is used for the ss case; a named list is
merged per-label for the bridge case.
}
