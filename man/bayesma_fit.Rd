% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesma_pipeline.R
\name{bayesma_fit}
\alias{bayesma_fit}
\title{Compile and sample a bayesma model}
\usage{
bayesma_fit(
  spec,
  code = bayesma_stan_code(spec),
  stan_data = bayesma_stan_data(spec),
  chains = 4,
  iter_warmup = 1000,
  iter_sampling = 1000,
  adapt_delta = 0.95,
  seed = 1234,
  ...
)
}
\arguments{
\item{spec}{A \code{bayesma_spec} object.}

\item{code}{A \code{bayesma_stan_code} object, or a character scalar Stan
program. Defaults to \code{bayesma_stan_code(spec)}.}

\item{stan_data}{A Stan data list. Defaults to \code{bayesma_stan_data(spec)}.}

\item{chains, iter_warmup, iter_sampling, adapt_delta, seed}{MCMC settings.}

\item{...}{Passed to \code{cmdstanr::CmdStanModel$sample()}.}
}
\value{
A list with class \code{"bayesma_fit"} containing elements \code{fit} (the
cmdstanr \code{CmdStanMCMC}), \code{stan_code} (list), \code{stan_data} (list), and
(for PET-PEESE) \code{pet_peese} decision metadata.
}
\description{
Takes the Stan code and data produced by earlier stages and runs
\code{cmdstanr::sample()}. Handles the PET-PEESE decision rule (fits PET first,
then switches to PEESE when posterior evidence for an effect exceeds the
threshold).
}
