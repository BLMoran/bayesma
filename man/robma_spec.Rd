% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robma_pipeline.R
\name{robma_spec}
\alias{robma_spec}
\title{Build a RoBMA specification object}
\usage{
robma_spec(
  data,
  studyvar,
  event_ctrl = NULL,
  event_int = NULL,
  mean_ctrl = NULL,
  mean_int = NULL,
  sd_ctrl = NULL,
  sd_int = NULL,
  n_ctrl = NULL,
  n_int = NULL,
  likelihood = c("binomial", "gaussian", "poisson"),
  priors_effect = NULL,
  priors_effect_null = NULL,
  priors_heterogeneity = NULL,
  priors_heterogeneity_null = NULL,
  priors_bias = NULL,
  priors_bias_null = NULL,
  rescale_priors = 1,
  method = c("bridge", "ss"),
  bias_indicator = c("bias_corrected", "pet_peese", "selection_weight"),
  null_range = NULL,
  b_prior = NULL,
  p_bias_prior = NULL,
  p_cutoffs = c(0.025, 0.05),
  parallel = FALSE,
  chains = 4,
  iter_warmup = 1000,
  iter_sampling = 1000,
  adapt_delta = 0.95,
  seed = 1234,
  quiet = FALSE,
  custom_model = NULL,
  custom_data = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame with one row per study.}

\item{studyvar}{Character. Column name of the study identifier.}

\item{event_ctrl, event_int}{Character. Column names of event counts
(binomial / Poisson likelihoods).}

\item{mean_ctrl, mean_int, sd_ctrl, sd_int}{Character. Column names of arm
means and SDs (Gaussian likelihood).}

\item{n_ctrl, n_int}{Character. Column names of arm sample sizes.}

\item{likelihood}{Character. One of \code{"binomial"}, \code{"gaussian"},
\code{"poisson"}.}

\item{priors_effect, priors_effect_null, priors_heterogeneity, priors_heterogeneity_null, priors_bias, priors_bias_null}{Lists of \code{bayesma_prior} objects for the effect, heterogeneity and
publication-bias components (alternative and null). If \code{NULL}, the
RoBMA defaults are used.}

\item{rescale_priors}{Numeric. Scale factor applied to default priors.
Default \code{1}.}

\item{method}{Character. \code{"bridge"} (default) uses bridgesampling across
the full model grid; \code{"ss"} uses a single spike-and-slab Stan model.}

\item{bias_indicator}{Character. Spike-and-slab bias mechanism:
\code{"bias_corrected"}, \code{"pet_peese"}, or \code{"selection_weight"}.}

\item{null_range}{Numeric length-2 vector. Range treated as the point
null for the effect component.}

\item{b_prior}{Prior on the \code{b} slope for spike-and-slab bias correction.}

\item{p_bias_prior}{Prior on the bias inclusion probability.}

\item{p_cutoffs}{Numeric vector of one-sided p-value cutoffs for
selection-weight models. Default \code{c(0.025, 0.05)}.}

\item{parallel}{Logical. Fit the bridgesampling grid in parallel.}

\item{chains, iter_warmup, iter_sampling, adapt_delta, seed}{MCMC settings.}

\item{quiet}{Logical. Suppress per-step progress messages.}

\item{custom_model}{Optional character scalar of Stan code overriding
the generated program (spike-and-slab only).}

\item{custom_data}{Optional named list merged into the Stan data list.}

\item{...}{Additional arguments forwarded to
\code{cmdstanr::CmdStanModel$sample()}.}
}
\value{
An object of class \code{c("bayesma_robma_spec", "bayesma_spec")}.
}
\description{
Build a RoBMA specification object
}
