% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecdf_model_plot.R
\name{ecdf_model_plot}
\alias{ecdf_model_plot}
\title{ECDF Plot Comparing Model Strategies}
\usage{
ecdf_model_plot(
  model,
  data,
  prior,
  estimand,
  rob_var = NULL,
  exclude_high_rob = FALSE,
  incl_common_effect = FALSE,
  incl_random_effect = TRUE,
  incl_bias_corrected = FALSE,
  incl_selection_copas = FALSE,
  incl_selection_weight = FALSE,
  incl_pet_peese = FALSE,
  incl_robust = FALSE,
  incl_robma = FALSE,
  prob_reference = "null",
  null_value = NULL,
  null_range = NULL,
  add_null_range = FALSE,
  color_null_range = "#77bb41",
  label_control = "Control",
  label_intervention = "Intervention",
  title = NULL,
  subtitle = NULL,
  xlim = NULL,
  x_breaks = NULL,
  color_palette = NULL,
  show_density = TRUE,
  font = NULL
)
}
\arguments{
\item{model}{A fitted \code{bayesma} object.}

\item{data}{A data frame containing the study data used to fit the model.}

\item{prior}{A list containing the prior specification with elements:
\describe{
\item{\code{mu_prior}}{(Required) Prior specification for mu.}
\item{\code{tau_prior}}{(Optional) Prior specification for tau.}
\item{\code{name}}{Display label for the prior. Required when
\code{incl_robma = TRUE} as it is used to match against priors in
\code{model$robma_sensitivity}. If not provided and
\code{incl_robma = FALSE}, defaults to "User-specified prior".}
}}

\item{estimand}{Effect estimand string (e.g., "OR", "RR", "HR", "IRR", "MD", "SMD").}

\item{rob_var}{Optional. Name of the risk-of-bias variable (unquoted).}

\item{exclude_high_rob}{Logical. If TRUE, includes an "Excluding High RoB" section.}

\item{incl_common_effect}{Logical. Include common effect model. Default FALSE.}

\item{incl_random_effect}{Logical. Include random effects model. Default TRUE.}

\item{incl_bias_corrected}{Logical. Include bias-corrected model. Default FALSE.}

\item{incl_selection_copas}{Logical. Include Copas selection model. Default FALSE.}

\item{incl_selection_weight}{Logical. Include weight-function selection model.
Default FALSE.}

\item{incl_pet_peese}{Logical. Include PET-PEESE model. Default FALSE.}

\item{incl_robust}{Logical. Include robust mixture model. Default FALSE.}

\item{incl_robma}{Logical. Include RoBMA model. Requires
\code{model$robma_sensitivity} to be present and \code{prior$name} to match
one of the priors used when fitting. Default FALSE.}

\item{prob_reference}{Character string. What reference to use for
probability axis labels. Either \code{"null"} (compare to
\code{null_value}) or \code{"null_range"} (compare to
\code{null_range} boundaries). Default is \code{"null"}.}

\item{null_value}{Null hypothesis value. If NULL, uses estimand default.}

\item{null_range}{Numeric vector of length 2 giving null range bounds.}

\item{add_null_range}{Logical. If TRUE and \code{null_range} is NULL,
uses estimand-appropriate defaults.}

\item{color_null_range}{Fill colour for the null range band.
Default \code{"#77bb41"}.}

\item{label_control}{Label for control group. Default \code{"Control"}.}

\item{label_intervention}{Label for intervention group. Default
\code{"Intervention"}.}

\item{title}{Optional plot title.}

\item{subtitle}{Optional plot subtitle. If NULL and a prior name is available,
displays the prior name.}

\item{xlim}{Optional x-axis limits.}

\item{x_breaks}{Optional x-axis break points.}

\item{color_palette}{Optional named vector of colours for model sections.}

\item{show_density}{Logical. If TRUE, includes a density plot
below the ECDF. Default is TRUE.}

\item{font}{Optional font family.}
}
\value{
A ggplot object (or patchwork object if \code{show_density = TRUE}).
}
\description{
Creates an empirical cumulative distribution function (ECDF) plot comparing
posterior distributions across different model strategies (e.g., random effects,
selection models, PET-PEESE) under a single prior specification.

Use this plot to assess how robust conclusions are across different
modelling assumptions.
}
\details{
The plot shows one ECDF line per model strategy, all using the same prior.
This allows direct comparison of how different modelling assumptions
(random effects, selection models, bias correction, etc.) affect the
posterior distribution.

The left y-axis shows P(effect < x) and the right y-axis shows P(effect > x).
}
\examples{
\dontrun{
priors <- list(
  vague = list(
    name = "Vague",
    mu_prior = normal(0, 10),
    tau_prior = half_cauchy(0, 1)
  ),
  weak_reg = list(
    name = "Weakly Regularising",
    mu_prior = normal(0, 1),
    tau_prior = half_cauchy(0, 0.5)
  )
)

ecdf_model_plot(
  model = model,
  data = dat,
  estimand = "OR",
  prior = priors$weak_reg,
  incl_random_effect = TRUE,
  incl_pet_peese = TRUE
)

ecdf_model_plot(
  model = model,
  data = dat,
  estimand = "OR",
  prior = priors$weak_reg,
  incl_robma = TRUE
)
}

}
\seealso{
\code{\link{ecdf_prior_plot}} for comparing priors within a model type.
}
