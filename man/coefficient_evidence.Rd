% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_reg_helpers.R
\name{coefficient_evidence}
\alias{coefficient_evidence}
\title{Bayesian Evidence for Meta-Regression Coefficients}
\usage{
coefficient_evidence(object, null_range = NULL, ci_level = 0.95)
}
\arguments{
\item{object}{A \code{bayesma_reg} object.}

\item{null_range}{Numeric vector of length 2 defining the null range, or NULL
(default) to skip null range calculation. For standardized coefficients,
\code{c(-0.1, 0.1)} is a common choice.}

\item{ci_level}{Numeric. Credible interval width (default: 0.95).}
}
\value{
A tibble with columns:
\itemize{
\item \code{term}: Coefficient name
\item \code{estimate}: Posterior median
\item \code{std_error}: Posterior SD
\item \code{ci_lower}, \code{ci_upper}: Credible interval bounds
\item \code{pd}: Probability of direction (max of P(β>0), P(β<0))
\item \code{direction}: Most probable direction ("positive" or "negative")
\item \code{p_null}: Proportion of posterior inside null range (if specified
\item \code{p_outside_null}: Proportion outside null range (if specified)
}
}
\description{
Computes Bayesian measures of evidence for moderator coefficients,
including probability of direction, null range (Region of Practical Equivalence),
and credible intervals.
}
\details{
\subsection{Probability of Direction (pd)}{

The probability of direction is the proportion of the posterior on
the same side of zero as the median. It ranges from 0.5 (no evidence)
to 1 (strong evidence for direction). A pd > 0.95 is often considered
meaningful evidence, though this is not a formal threshold.
}

\subsection{null range (Region of Practical Equivalence)}{

The null range defines a range of values considered "practically equivalent
to zero". The proportion of the posterior inside the null range indicates
whether the effect is practically meaningful:
\itemize{
\item \code{p_null} > 0.95: Effect is practically zero (accept null)
\item \code{p_null} < 0.05: Effect is practically meaningful (reject null)
\item Otherwise: Inconclusive
}

Common null range choices:
\itemize{
\item \code{c(-0.1, 0.1)} for standardized coefficients (Cohen's d scale)
\item \code{c(-0.05, 0.05)} for log-OR (corresponds to OR 0.95-1.05)
}
}
}
\examples{
\dontrun{
fit <- meta_reg(data, studyvar = "author", yi = "yi", vi = "vi",
                mods = ~ year + quality)

# Basic evidence summary
coefficient_evidence(fit)

# With null range
coefficient_evidence(fit, null_range = c(-0.1, 0.1))
}

}
