% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{interpret}
\alias{interpret}
\title{Interpret a Bayesian meta-analysis workflow}
\usage{
interpret(
  ...,
  null_range = c(-0.1, 0.1),
  effect_label = NULL,
  credible_level = 0.95,
  quiet = FALSE
)
}
\arguments{
\item{...}{One or more fitted bayesma objects (named or unnamed). Accepted
classes: \code{bayesma}, \code{bayesma_mv}, \code{bayesma_robma}, \code{bayesma_egger},
\code{bayesma_metareg}, \code{bayesma_robma_sensitivity}, \code{bayesma_comparison}.}

\item{null_range}{Optional length-2 numeric vector for direction/ROPE
probabilities. Defaults to \code{c(-0.1, 0.1)} on the natural scale.}

\item{effect_label}{Optional character override for the effect label used
in narrative text (e.g. "log_or"). Inherited from fits when \code{NULL}.}

\item{credible_level}{Credible interval width used in summaries.
Default \code{0.95}.}

\item{quiet}{If \code{TRUE}, suppresses progress messages during assembly.}
}
\value{
An object of class \code{bayesma_interpretation} -- a list with one
element per detected section plus a \code{meta} slot. The \code{print()} method
renders the full narrative report.
}
\description{
Generates a comprehensive narrative interpretation across one or more
bayesma fits -- overall effects, heterogeneity, publication bias, model
averaging, sensitivity, model comparison, and convergence diagnostics.
The function auto-detects what is provided and assembles only the
relevant sections.
}
\examples{
\dontrun{
fit  <- bayesma::bayesma(data = dat, yi = "yi", sei = "sei")
rob  <- bayesma::robma(data = dat, yi = "yi", sei = "sei")
egg  <- bayesma::egger(data = dat, yi = "yi", sei = "sei")
sens <- bayesma::robma_sensitivity(data = dat, priors = my_priors)
interpret(fit, rob, egg, sens)
}
}
