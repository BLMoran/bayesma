% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesma.R
\name{bayesma}
\alias{bayesma}
\title{Run a Bayesian Meta-Analysis in Stan}
\usage{
bayesma(
  data,
  studyvar,
  event_ctrl = NULL,
  event_int = NULL,
  mean_ctrl = NULL,
  mean_int = NULL,
  sd_ctrl = NULL,
  sd_int = NULL,
  n_ctrl = NULL,
  n_int = NULL,
  likelihood = c("binomial", "gaussian", "poisson"),
  model_type = c("random_effect", "common_effect", "bias_corrected", "bc_bnp",
    "selection_copas", "selection_weight", "pet_peese", "mixture_model"),
  stage = c("one_stage", "two_stage"),
  re_dist = c("normal", "t", "skew_normal", "mixture"),
  small_sample = c("none", "t_approx", "hjsk"),
  multi_arm = NULL,
  rho_prior = NULL,
  mu_prior = NULL,
  tau_prior = NULL,
  gamma_prior = NULL,
  nu_prior = NULL,
  alpha_prior = NULL,
  mixture_priors = NULL,
  b_prior = NULL,
  p_bias_prior = NULL,
  w_bias_prior = NULL,
  mu_beta_prior = NULL,
  tau_beta_prior = NULL,
  bnp_concentration_max = NULL,
  bnp_K_max = NULL,
  use_known_bias = FALSE,
  selection_priors = NULL,
  p_cutoffs = c(0.025, 0.05),
  n_components = 2L,
  robust = FALSE,
  robust_prior = NULL,
  robust_df = 4,
  robust_weight = NULL,
  custom_model = NULL,
  custom_data = NULL,
  estimand = NULL,
  cate_covariate = NULL,
  baseline_risk = NULL,
  re_min_k = NULL,
  sample_prior = FALSE,
  chains = 4,
  iter_warmup = 1000,
  iter_sampling = 1000,
  adapt_delta = 0.95,
  seed = 1234,
  ...
)
}
\arguments{
\item{data}{A data frame with one row per study (or per arm for multi-arm studies).}

\item{studyvar}{Character. Column name for study identifiers.}

\item{event_ctrl, event_int}{Character (binomial/poisson). Event counts.}

\item{mean_ctrl, mean_int}{Character (gaussian). Arm means.}

\item{sd_ctrl, sd_int}{Character (gaussian). Arm SDs.}

\item{n_ctrl, n_int}{Character. Arm sample sizes.}

\item{likelihood}{Character. \code{"binomial"}, \code{"gaussian"}, or \code{"poisson"}.}

\item{model_type}{Character. \code{"random_effect"}, \code{"common_effect"},
\code{"bias_corrected"}, \code{"bc_bnp"} (Bias-Corrected Bayesian Non-Parametric;
Verde 2025), \code{"selection_copas"} (Robust Bayesian Copas; Bai 2020),
\code{"selection_weight"}, \code{"pet_peese"}, or \code{"mixture_model"} (Bayesian
meta-analytic mixture; Maier 2024).}

\item{stage}{Character. \code{"one_stage"} or \code{"two_stage"}.}

\item{re_dist}{Character. \code{"normal"}, \code{"t"}, \code{"skew_normal"}, or \code{"mixture"}.
For \code{model_type = "selection_copas"}, only \code{"normal"} and \code{"t"} are
supported; pass \code{"t"} for the Bai (2020) Robust Bayesian Copas (RBC)
formulation with heavy-tailed random effects (recommended; see
\code{nu_prior}).}

\item{small_sample}{Character. \code{"none"}, \code{"t_approx"}, or \code{"hjsk"}.}

\item{multi_arm}{Character or NULL. Column name indicating multi-arm study
grouping. Only used with \code{stage = "one_stage"}.}

\item{rho_prior}{Prior on the within-study correlation for multi-arm studies.}

\item{mu_prior}{Prior on the pooled effect mu.}

\item{tau_prior}{Prior on the RE standard deviation tau.}

\item{gamma_prior}{Prior on study baselines gamma. One-stage only.}

\item{nu_prior}{Prior on RE degrees of freedom nu. \code{re_dist = "t"} only.}

\item{alpha_prior}{Prior on RE skewness shape. \code{re_dist = "skew_normal"} only.}

\item{mixture_priors}{Named list of priors for mixture components.}

\item{b_prior}{Prior on bias shift B. \code{model_type = "bias_corrected"} only.}

\item{p_bias_prior}{Prior on proportion of biased studies. Used by
\code{bias_corrected} and \code{bc_bnp}.}

\item{w_bias_prior}{Prior on per-study bias weight. \code{bias_corrected} only.}

\item{mu_beta_prior}{Prior on the bias-component location (DP base). Used
by \code{bc_bnp} only. Default \code{uniform(-15, 15)}.}

\item{tau_beta_prior}{Prior on the bias-component scale (DP base). Used
by \code{bc_bnp} only. Default \code{half_cauchy(0, 1)}.}

\item{bnp_concentration_max}{Numeric. Upper bound for the DP concentration
\code{alpha ~ Uniform(0.5, bnp_concentration_max)}. \code{bc_bnp} only. Default
\code{NULL} resolves from data via Verde (2025) eqn (20).}

\item{bnp_K_max}{Integer. Truncation level for the stick-breaking
approximation. \code{bc_bnp} only. Default \code{NULL} resolves to
\code{1 + ceiling(5 * bnp_concentration_max)}.}

\item{use_known_bias}{Logical. If \code{TRUE}, \code{data} must contain a \code{biased} column.}

\item{selection_priors}{Named list of priors for selection model parameters.}

\item{p_cutoffs}{Numeric vector. One-sided p-value cutpoints for the
weight-function selection model.}

\item{n_components}{Integer. Number of mixture components.}

\item{robust}{Logical. Adds a two-component outlier mixture.}

\item{robust_prior}{Prior on the outlier component location.}

\item{robust_df}{Degrees of freedom for the outlier component.}

\item{robust_weight}{Prior on the mixing weight for the main component.}

\item{custom_model}{Optional Stan program (character scalar). When non-NULL,
code generation is bypassed and the user's program is used verbatim.}

\item{custom_data}{Optional named list of Stan data entries that override or
augment the automatically built data list.}

\item{estimand}{Character. The user-facing target effect:
\code{"OR"}, \code{"RR"}, \code{"HR"}, \code{"IRR"}, \code{"MD"}, \code{"SMD"} (relative-effect /
mean-difference estimands; behave as today), or \code{"RD"} / \code{"ARR"},
\code{"ATE"}, \code{"ATT"}, \code{"CATE"} (marginal estimands; computed from posterior
draws via \code{\link[=bayesma_marginal]{bayesma_marginal()}} and attached to the output as \verb{$marginal}).
Default \code{NULL} infers from \code{likelihood}.}

\item{cate_covariate}{Character. Column name of a study-level covariate to
condition on for \code{estimand = "CATE"}. Required when CATE is requested.}

\item{baseline_risk}{Numeric, between 0 and 1, or \code{"study_mean"}. Reference
baseline risk used to back-transform relative effects to absolute scale
for two-stage binomial fits with \code{estimand} in \code{c("RD","ARR","ATE","ATT")}.
Default \code{NULL} falls back to the observed control-arm event rate.}

\item{re_min_k}{Optional numeric. If set and \code{model_type = "random_effect"},
the model is downgraded to \code{"common_effect"} with a warning when the
number of unique studies is below \code{re_min_k}. Default \code{NULL} (no
enforcement). Useful for subgroup analyses where some strata have too few
studies for stable random-effects estimation.}

\item{sample_prior}{Logical. If \code{TRUE}, samples from the prior predictive
distribution by fitting the model without conditioning on the data
likelihood. The result can be passed to \code{\link[=pp_check]{pp_check()}} to verify that the
prior places mass on plausible observable values. Use this for prior
elicitation and sanity-checking — not for calibrating priors against the
analysis dataset itself (which would be circular). Default \code{FALSE}.}

\item{chains, iter_warmup, iter_sampling, adapt_delta, seed}{MCMC settings.}

\item{...}{Passed to \code{cmdstanr::sample()}.}
}
\value{
A list of class \code{"bayesma"}.
}
\description{
Fits a Bayesian meta-analysis model via Stan. Use \code{stan_code(model)} to
inspect the generated Stan program after fitting. Supply \code{custom_model} to
use your own Stan program instead.
}
