% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecdf_prior_plot.R
\name{ecdf_prior_plot}
\alias{ecdf_prior_plot}
\title{ECDF Plot Comparing Prior Sensitivity}
\usage{
ecdf_prior_plot(
  model,
  data,
  priors,
  estimand,
  model_types = "random_effect",
  prior_order = NULL,
  prob_reference = "null",
  null_value = NULL,
  null_range = NULL,
  add_null_range = FALSE,
  color_null_range = "#77bb41",
  label_control = "Control",
  label_intervention = "Intervention",
  title = NULL,
  subtitle = NULL,
  xlim = NULL,
  x_breaks = NULL,
  color_palette = NULL,
  linetype_by_model = TRUE,
  show_density = TRUE,
  font = NULL
)
}
\arguments{
\item{model}{A fitted \code{bayesma} object.}

\item{data}{A data frame containing the study data used to fit the model.}

\item{priors}{A named list of prior specifications. Each element must be a
list with at least \code{mu_prior} and optionally \code{tau_prior}, and
may include \code{name} for display labels.}

\item{estimand}{Effect estimand string (e.g., "OR", "RR", "HR", "IRR", "MD", "SMD").}

\item{model_types}{Character vector specifying which model type(s) to include.
Maximum of 2 model types to avoid clutter. Valid values: \code{"common_effect"},
\code{"random_effect"}, \code{"bias_corrected"}, \code{"selection_copas"},
\code{"selection_weight"}, \code{"pet_peese"}, \code{"robust"}, \code{"robma"}.
Default is \code{"random_effect"}.}

\item{prior_order}{Optional character vector specifying the display order of
priors. Should contain the names (IDs) of the priors in the desired order.
If NULL, priors are displayed in the order they appear in \code{priors}.}

\item{prob_reference}{Character string. What reference to use for
probability axis labels. Either \code{"null"} (compare to
\code{null_value}) or \code{"null_range"} (compare to
\code{null_range} boundaries). Default is \code{"null"}.}

\item{null_value}{Null hypothesis value. If NULL, uses estimand default.}

\item{null_range}{Numeric vector of length 2 giving null range bounds.}

\item{add_null_range}{Logical. If TRUE and \code{null_range} is NULL,
uses estimand-appropriate defaults.}

\item{color_null_range}{Fill colour for the null range band.
Default \code{"#77bb41"}.}

\item{label_control}{Label for control group. Default \code{"Control"}.}

\item{label_intervention}{Label for intervention group. Default
\code{"Intervention"}.}

\item{title}{Optional plot title.}

\item{subtitle}{Optional plot subtitle. If NULL and a single model type is
selected, displays the model type.}

\item{xlim}{Optional x-axis limits.}

\item{x_breaks}{Optional x-axis break points.}

\item{color_palette}{Optional named vector of colours for priors.}

\item{linetype_by_model}{Logical. If TRUE and multiple model types are
selected, uses different linetypes for each model type. Default TRUE.}

\item{show_density}{Logical. If TRUE, includes a density plot
below the ECDF. Default is TRUE.}

\item{font}{Optional font family.}
}
\value{
A ggplot object (or patchwork object if \code{show_density = TRUE}).
}
\description{
Creates an empirical cumulative distribution function (ECDF) plot comparing
posterior distributions across different prior specifications for a single
model type (or small set of model types).

Use this plot to assess how sensitive conclusions are to the choice of
prior distributions.
}
\details{
The plot shows one ECDF line per prior specification (and per model type if
multiple are selected). This allows direct comparison of how different
prior choices affect the posterior distribution.

When two model types are selected, colour represents prior and linetype
represents model type, allowing comparison of both dimensions simultaneously.

The left y-axis shows P(effect < x) and the right y-axis shows P(effect > x).
}
\seealso{
\code{\link{ecdf_model_plot}} for comparing models within a prior.
}
