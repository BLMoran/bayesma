% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_reg_plots.R
\name{bubble_plot}
\alias{bubble_plot}
\title{Bubble Plot for Meta-Regression}
\usage{
bubble_plot(
  object,
  mod,
  ci = TRUE,
  ci_level = 0.95,
  size_scale = 1,
  show_studies = FALSE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  color_palette = c("#4292C6", "#08519C"),
  theme = ggplot2::theme_minimal()
)
}
\arguments{
\item{object}{A \code{bayesma_reg} object.}

\item{mod}{Character. Name of the moderator variable to plot.
Must be a continuous moderator.}

\item{ci}{Logical. Show confidence/credible band for the regression
line (default: TRUE).}

\item{ci_level}{Numeric. Credible interval level (default: 0.95).}

\item{size_scale}{Numeric. Scaling factor for bubble sizes (default: 1).}

\item{show_studies}{Logical. Label study points (default: FALSE).}

\item{xlab, ylab}{Character. Axis labels. If NULL, uses variable names.}

\item{title}{Character. Plot title.}

\item{color_palette}{Character vector of length 2. Colors for points
and regression line.}

\item{theme}{A ggplot2 theme (default: \code{theme_minimal()}).}
}
\value{
A ggplot object.
}
\description{
Creates a bubble plot showing the relationship between a continuous
moderator and the effect size, with point sizes proportional to
study precision (inverse variance).
}
\details{
The bubble plot is a standard visualization for meta-regression with
continuous moderators. Each study is represented by a circle:
\itemize{
\item \strong{Position}: x = moderator value, y = effect size
\item \strong{Size}: Proportional to study weight (1 / variance)
\item \strong{Line}: Regression line showing the moderator effect
\item \strong{Band}: Credible interval for the regression line
}

For centered moderators, the x-axis shows the centered values by default.
Use \code{centered = FALSE} in the original \code{meta_reg()} call if you want
the original scale.
}
\examples{
\dontrun{
fit <- meta_reg(data, studyvar = "author", yi = "yi", vi = "vi",
                mods = ~ year + dose)

# Basic bubble plot
bubble_plot(fit, mod = "year")

# Customized plot
bubble_plot(fit, mod = "dose",
            xlab = "Dose (mg)",
            ylab = "Log Odds Ratio",
            title = "Dose-Response Relationship")
}

}
