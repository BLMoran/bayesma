% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_reg_plots.R
\name{metareg_mod_plot}
\alias{metareg_mod_plot}
\title{Plot Method for bayesma_coef_evidence Objects}
\usage{
metareg_mod_plot(
  x,
  model = NULL,
  include_intercept = FALSE,
  show_null_range = TRUE,
  null_value = 0,
  output = c("density", "pointinterval"),
  split_color_by_null = FALSE,
  color_posterior = "dodgerblue",
  color_posterior_outline = "blue",
  color_favours_positive = "dodgerblue",
  color_favours_negative = "firebrick",
  color_null_range = "grey50",
  color_pointinterval = "blue",
  xlim = NULL,
  x_breaks = NULL,
  xlab = "Coefficient Estimate",
  title = "Meta-Regression Coefficients",
  subtitle = NULL,
  add_table = TRUE,
  table_width = 0.4,
  font = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{bayesma_coef_evidence} object (created by \code{coefficient_evidence()}).}

\item{model}{The \code{bayesma_metareg} model. Required to extract posterior
draws for density slabs.}

\item{include_intercept}{Logical. Include intercept in plot (default: FALSE).}

\item{show_null_range}{Logical. Show null range region if available (default: TRUE).}

\item{null_value}{Numeric. Value for the null reference line (default: 0).}

\item{output}{Character. Either "density" (default) for density slabs or
"pointinterval" for point + interval display.}

\item{split_color_by_null}{Logical. If TRUE, posterior densities are split and
coloured based on whether values fall above or below the null value (default: FALSE).}

\item{color_posterior}{Color for coefficient posterior densities (default: "dodgerblue").}

\item{color_posterior_outline}{Color for posterior outlines (
default: "blue").}

\item{color_favours_positive}{Colour for density regions > null when
\code{split_color_by_null = TRUE} (default: "dodgerblue").}

\item{color_favours_negative}{Colour for density regions < null when
\code{split_color_by_null = TRUE} (default: "firebrick").}

\item{color_null_range}{Color for null range shading (default: "grey50").}

\item{color_pointinterval}{Color for point intervals when \code{output = "pointinterval"}
(default: "blue").}

\item{xlim}{Numeric vector of length 2 specifying x-axis limits. Default is NULL
(auto-scaled).}

\item{x_breaks}{Numeric vector specifying custom x-axis break points. Default
is NULL (auto).}

\item{xlab}{Character. X-axis label (default: "Coefficient Estimate").}

\item{title}{Character. Plot title (default: "Meta-Regression Coefficients").}

\item{subtitle}{Character. Plot subtitle. Default shows null range if provided.}

\item{add_table}{Logical. Add a table with coefficient summaries on the right
(default: TRUE).}

\item{table_width}{Numeric. Relative width of the table vs plot (default: 0.4).}

\item{font}{Character. Font family for text elements (default: NULL).}

\item{...}{Additional arguments (unused).}
}
\value{
A ggplot object (or patchwork object if \code{add_table = TRUE}).
}
\description{
Creates a forest-style plot for meta-regression coefficients using
posterior density slabs, consistent with the \code{forest} visual style.
}
\details{
This function creates a forest-style visualisation for meta-regression
coefficients that matches the aesthetic of \code{forest()}. Each coefficient
is displayed as a posterior density slab (or point interval), allowing
visualisation of the full posterior distribution rather than just point
estimates and credible intervals

When \code{split_color_by_null = TRUE}, the density is split at the null value
and coloured to show the proportion of the posterior on each side.
}
\examples{
\dontrun{
fit <- meta_reg(data, studyvar = "author", yi = "yi", vi = "vi",
                mods = ~ year + quality)

# Get evidence summary
ev <- coefficient_evidence(fit, null_range = c(-0.1, 0.1))

# Basic forest-style plot
metareg_mod_plot(ev, model = fit)

# With split coloring
metareg_mod_plot(ev, model = fit, split_color_by_null = TRUE)

# Point interval style
metareg_mod_plot(ev, model = fit, output = "pointinterval")
}

}
