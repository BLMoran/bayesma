% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_check.R
\name{pp_check}
\alias{pp_check}
\title{Posterior and Prior Predictive Checks for bayesma Objects}
\usage{
pp_check(object, type = "dens_overlay", ndraws = 100, ...)
}
\arguments{
\item{object}{A \code{bayesma} object. If fitted with \code{sample_prior = TRUE},
a prior predictive check is produced; otherwise a posterior predictive
check.}

\item{type}{Character. Plot type passed to \verb{bayesplot::ppc_*}. One of
\code{"dens_overlay"} (default), \code{"hist"}, \code{"stat"}, \code{"scatter"},
\code{"bars"} (discrete only), \code{"ecdf_overlay"}, \code{"ribbon"}.}

\item{ndraws}{Integer. Number of draws to display. Default \code{100}.}

\item{...}{Additional arguments passed to the underlying
\verb{bayesplot::ppc_*} function.}
}
\value{
A \code{ggplot} object.
}
\description{
Compares observed data to replicated datasets drawn from either the
posterior predictive distribution (standard use) or the prior predictive
distribution (when the model was fitted with \code{sample_prior = TRUE}).

\strong{Posterior predictive check} (\code{sample_prior = FALSE}, the default): draws
\code{y_rep} from \code{p(y_rep | y)}. Agreement between \code{y} and \code{y_rep} indicates the
fitted model can reproduce the observed data.

\strong{Prior predictive check} (\code{sample_prior = TRUE}): draws \code{y_rep} from
\code{p(y_rep)}, integrating over the prior without conditioning on the data.
This is a tool for prior \emph{elicitation and sanity-checking} — verifying that
the prior does not place substantial mass on data values that are impossible
or implausible given domain knowledge, before the data have been seen.
}
\section{Prior predictive checks and double-dipping}{

It is legitimate to use a prior predictive check to verify that a prior is
coherent on the observable scale (e.g. that it does not imply impossible
event counts). It is \strong{not} legitimate to iteratively adjust priors until
\code{y_rep} matches the observed \code{y}: doing so smuggles the data into the prior,
inflating posterior confidence. Priors should be specified from external
knowledge, expert elicitation, or independent reference data — not from the
analysis dataset itself.
}

\examples{
\dontrun{
# Posterior predictive check
fit <- bayesma(data, likelihood = "binomial", ...)
pp_check(fit)
pp_check(fit, type = "stat", stat = "mean")

# Prior predictive check — use to verify priors are sensible,
# not to calibrate them against the analysis data
prior_fit <- bayesma(data, likelihood = "binomial", ..., sample_prior = TRUE)
pp_check(prior_fit)
}

}
