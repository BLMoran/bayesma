% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect_sr.R
\name{inspect_sr}
\alias{inspect_sr}
\title{Run the INSPECT-SR Trustworthiness Assessment}
\usage{
inspect_sr(
  data,
  studyvar = study,
  bayes = FALSE,
  prior_prob_trustworthy = 0.9,
  pvalue_tolerance = 0.01,
  carlisle_method = "fisher",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame or tibble with one row per study. See
\strong{Expected columns} in the package vignette, or the bundled
\link{inspect_sr_example} dataset for the exact layout.}

\item{studyvar}{Unquoted column name identifying the study (tidyeval).
Defaults to \code{study}.}

\item{bayes}{Logical. If \code{FALSE} (default) produces frequentist pass/fail
judgements. If \code{TRUE} produces Bayes factors and a posterior probability
of trustworthiness.}

\item{prior_prob_trustworthy}{Numeric in (0, 1). Prior probability that
each study is trustworthy, used only when \code{bayes = TRUE} (default 0.90).}

\item{pvalue_tolerance}{Numeric. Tolerance for the frequentist p-value
check (default 0.01).}

\item{carlisle_method}{\code{"fisher"} (default) or \code{"ks"} — see
\code{\link[=carlisle_test]{carlisle_test()}}.}

\item{verbose}{Logical. Print a summary to the console (default \code{TRUE}).}
}
\value{
If \code{bayes = FALSE}: an object of class \code{inspect_sr} (a data frame with
columns \code{Study}, \code{D1}, \code{D2}, \code{D3}, \code{D4}, \code{Overall}), with per-study
details in \code{attr(x, "details")}.

If \code{bayes = TRUE}: an object of class \code{bayes_inspect_sr} (a data frame
with columns \code{Study}, \code{Prior}, \code{Posterior}, \code{Combined_BF},
\code{Interpretation}), with individual Bayes factors in \code{attr(x, "details")}.
}
\description{
Takes a data frame with one row per study and runs the automated Domain 4
checks (Carlisle's test, participant-number consistency, GRIM, p-value
verification). Manual items (D1, D2, D3, and the non-automated D4 items)
are read straight from the input. Domain-level and overall judgements are
derived per INSPECT-SR guidance (overall = most severe domain).
}
\examples{
\dontrun{
data(inspect_sr_example)

# Frequentist
res <- inspect_sr(inspect_sr_example, studyvar = study)

# Bayesian
res_bayes <- inspect_sr(inspect_sr_example, studyvar = study, bayes = TRUE)
}

}
\seealso{
\code{\link[=inspect_sr_table]{inspect_sr_table()}} for a per-check gt table;
\code{\link[=inspect_plot]{inspect_plot()}} for the traffic-light visualisation;
\code{\link[=filter_trustworthy]{filter_trustworthy()}} for filtering a meta-analysis dataset.
}
