% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesma_marginal.R
\name{bayesma_marginal}
\alias{bayesma_marginal}
\title{Compute a marginal estimand from a bayesma fit}
\usage{
bayesma_marginal(fit, spec)
}
\arguments{
\item{fit}{A \code{bayesma_fit} object (or the cmdstanr fit inside one).}

\item{spec}{The matching \code{bayesma_spec}.}
}
\value{
A list with elements:
\describe{
\item{\code{estimand}}{The estimand label.}
\item{\code{draws}}{A numeric vector of posterior draws on the natural scale.}
\item{\code{summary}}{A tibble with median, 95\\% CrI, and posterior probability
of being above zero.}
}
}
\description{
Post-processes the posterior draws of a fitted \code{\link[=bayesma]{bayesma()}} model to return
a marginal estimand on the natural scale: risk difference (RD/ARR), average
treatment effect (ATE), average treatment effect on the treated (ATT), or
a conditional average treatment effect (CATE).
}
\details{
For relative-effect estimands (\code{"OR"}, \code{"RR"}, \code{"HR"}, \code{"IRR"}, \code{"MD"},
\code{"SMD"}) this function is unnecessary — \code{\link[=bayesma_output]{bayesma_output()}} already returns
the pooled effect on the appropriate scale.
}
\section{Methods by estimand and stage}{


\describe{
\item{RD / ARR / ATE — binomial, one-stage}{Computed from posterior draws
of the per-arm linear predictor. Mean over studies of
\code{inv_logit(eta_int) - inv_logit(eta_ctrl)}.}
\item{RD / ARR / ATE — binomial, two-stage}{Requires \code{baseline_risk}.
Posterior draws of the pooled OR are back-transformed at the supplied
baseline. Defaults to the unweighted mean of the observed control rates
when \code{baseline_risk = NULL}.}
\item{ATE — gaussian}{Equivalent to MD. Returns the pooled posterior on
the absolute scale.}
\item{ATT}{Computed as ATE weighted by treated-arm sample size
(\code{n_int / sum(n_int)}). Without IPD this is an arm-size-weighted ATE,
not a true causal ATT — interpret with caution.}
\item{CATE}{Routes to \code{\link[=meta_reg]{meta_reg()}} with \code{moderators = cate_covariate}.
Reports the meta-regression effect; the user is expected to evaluate
it at a specific covariate value downstream.}
}
}

