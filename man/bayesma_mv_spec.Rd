% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesma_mv.R
\name{bayesma_mv_spec}
\alias{bayesma_mv_spec}
\title{Build a bivariate meta-analysis specification object}
\usage{
bayesma_mv_spec(
  data,
  studyvar,
  mean_ctrl_1,
  mean_int_1,
  sd_ctrl_1,
  sd_int_1,
  n_ctrl_1,
  n_int_1,
  mean_ctrl_2,
  mean_int_2,
  sd_ctrl_2,
  sd_int_2,
  n_ctrl_2,
  n_int_2,
  outcome_labels = c("outcome_1", "outcome_2"),
  likelihood = c("gaussian"),
  stage = c("two_stage", "one_stage"),
  rho_within = 0.5,
  mu_prior = NULL,
  tau_prior = NULL,
  rho_between_prior = NULL,
  custom_model = NULL,
  custom_data = NULL
)
}
\arguments{
\item{data}{A data frame with one row per study containing arm-level data
for both outcomes.}

\item{studyvar}{Column for study identifiers (unquoted).}

\item{mean_ctrl_1, mean_int_1}{Columns for control and intervention means
for outcome 1 (unquoted).}

\item{sd_ctrl_1, sd_int_1}{Columns for control and intervention SDs for
outcome 1 (unquoted).}

\item{n_ctrl_1, n_int_1}{Columns for control and intervention sample sizes
for outcome 1 (unquoted).}

\item{mean_ctrl_2, mean_int_2}{Columns for control and intervention means
for outcome 2 (unquoted).}

\item{sd_ctrl_2, sd_int_2}{Columns for control and intervention SDs for
outcome 2 (unquoted).}

\item{n_ctrl_2, n_int_2}{Columns for control and intervention sample sizes
for outcome 2 (unquoted).}

\item{outcome_labels}{Character vector of length 2 with labels for the
two outcomes. Default: \code{c("outcome_1", "outcome_2")}.}

\item{likelihood}{Character. Currently only \code{"gaussian"}.}

\item{stage}{Character. \code{"two_stage"} (effect sizes computed then
modelled) or \code{"one_stage"} (marginalised model).}

\item{rho_within}{Numeric scalar in \verb{[-1, 1]}. Within-study correlation
between the two outcomes, assumed known. Default: \code{0.5}.}

\item{mu_prior}{Prior on pooled effects. Either a single prior (applied
to both outcomes) or a named list with elements matching
\code{outcome_labels}.}

\item{tau_prior}{Prior on between-study SDs. Either a single prior or a
named list.}

\item{rho_between_prior}{Prior on the between-study correlation.
Default: \code{uniform(-1, 1)}.}

\item{custom_model}{Optional character scalar containing complete Stan
code to override the generated program.}

\item{custom_data}{Optional named list merged into the Stan data list.}
}
\value{
An object of class \code{"bayesma_mv_spec"}.
}
\description{
Build a bivariate meta-analysis specification object
}
