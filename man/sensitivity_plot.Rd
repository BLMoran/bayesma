% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_plot.R
\name{sensitivity_plot}
\alias{sensitivity_plot}
\title{Generate Sensitivity Analysis Plot for Bayesian Meta-Analysis}
\usage{
sensitivity_plot(
  model,
  data,
  priors,
  measure,
  prior_order = NULL,
  model_order = NULL,
  rob_var = NULL,
  exclude_high_rob = FALSE,
  incl_common_effect = FALSE,
  incl_random_effect = TRUE,
  incl_bias_corrected = FALSE,
  incl_selection_copas = FALSE,
  incl_selection_weight = FALSE,
  incl_pet_peese = FALSE,
  incl_robust = FALSE,
  incl_robma = FALSE,
  parallel = FALSE,
  workers = NULL,
  seed = TRUE,
  add_probs = FALSE,
  null_value = NULL,
  null_range = NULL,
  add_null_range = FALSE,
  color_null_range = "#77bb41",
  label_control = "Control",
  label_intervention = "Intervention",
  title = NULL,
  subtitle = NULL,
  title_align = "left",
  xlim = NULL,
  x_breaks = NULL,
  color_palette = NULL,
  color_overall_posterior = "dodgerblue",
  color_overall_posterior_outline = "blue",
  split_color_by_null = FALSE,
  color_favours_control = "firebrick",
  color_favours_intervention = "dodgerblue",
  plot_width = 4,
  font = NULL
)
}
\arguments{
\item{model}{A fitted \code{bayesma} object. For RoBMA results, must have
\verb{$robma_sensitivity} attached via \code{attach_robma_sensitivity()}.}

\item{data}{A data frame containing the study data used to fit the model.}

\item{priors}{A named list of prior specifications. Each element must be a list
with at least \code{mu_prior} and (optionally) \code{tau_prior}, and may include
\code{name} used for display.}

\item{measure}{Effect measure string (e.g., "OR", "RR", "HR", "IRR", "MD", "SMD").}

\item{prior_order}{Optional character vector specifying the display order of priors.
Should contain the names (IDs) of the priors in the desired order,
e.g., \code{c("vague", "weak_reg", "informative")}. If NULL (default), priors
are displayed in the order they appear in the \code{priors} list.}

\item{model_order}{Optional. Specifies the display order of model sections.
Can be provided as unquoted names (using rlang) or as a character vector.

Valid values match the \verb{incl_*} argument names (without the \code{incl_} prefix):
\code{common_effect}, \code{random_effect}, \code{bias_corrected}, \code{selection_copas},
\code{selection_weight}, \code{pet_peese}, \code{robust}, \code{robma}.

For RoBMA, use \code{robma} to include both conditional and model-averaged estimates
(conditional first), or \code{robma_conditional} / \code{robma_model_averaged} separately.

Example: \code{model_order = c(common_effect, random_effect, pet_peese, robma)}

If NULL (default), sections are displayed in the default order.}

\item{rob_var}{Optional. Name of the risk-of-bias variable (unquoted).}

\item{exclude_high_rob}{Logical. If TRUE, runs an "Excluding High RoB" section.}

\item{incl_common_effect, incl_random_effect, incl_bias_corrected, incl_selection_copas, incl_selection_weight, incl_pet_peese, incl_robust}{Logical. Which model strategy sections to include.}

\item{incl_robma}{Logical. If TRUE, include RoBMA section. Requires
\code{model$robma_sensitivity} to be present (see Details).}

\item{parallel}{Logical. If TRUE, uses parallel processing for non-RoBMA refits.}

\item{workers}{Optional integer. Number of parallel workers.}

\item{seed}{Logical. If TRUE (default), uses parallel-safe seeding.}

\item{add_probs}{Logical. Add probability columns to the results table.}

\item{null_value, null_range, add_null_range, color_null_range}{ROPE settings.}

\item{label_control, label_intervention}{Group labels for the plot.}

\item{title, subtitle, title_align}{Title settings.}

\item{xlim, x_breaks}{Density axis settings.}

\item{color_palette, color_overall_posterior, color_overall_posterior_outline}{Colour settings.}

\item{split_color_by_null}{Logical. If TRUE, colour the posterior split at
the null value using \code{color_favours_control} and \code{color_favours_intervention}.}

\item{color_favours_control}{Colour for the side of the posterior favouring control.}

\item{color_favours_intervention}{Colour for the side favouring intervention.}

\item{plot_width}{Width ratio for the density plot section.}

\item{font}{Optional font family.}
}
\value{
A \code{bayesma_sensitivity_plot} object (patchwork combining tables and plots).
}
\description{
Creates a sensitivity analysis visualisation showing how meta-analytic
estimates vary across different model strategies and user-specified priors.

For RoBMA results, models must be pre-computed using \code{run_robma_sensitivity()}
and attached to the bayesma object before calling this function.
}
\details{
\subsection{RoBMA Results}{

To include RoBMA in the sensitivity plot, you must pre-compute the RoBMA fits:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Step 1: Run RoBMA sensitivity analysis
robma_sens <- run_robma_sensitivity(
  data = my_data,
  priors = my_priors,
  robma_template = my_robma_fit,
  parallel = TRUE
)

# Step 2: Attach to bayesma model
model <- attach_robma_sensitivity(model, robma_sens)

# Step 3: Create sensitivity plot with RoBMA
sensitivity_plot(model, data, priors, measure = "OR", incl_robma = TRUE)
}\if{html}{\out{</div>}}

This separation allows the computationally expensive RoBMA fitting to be done
once and cached, rather than being re-run every time the plot is generated.
}
}
