% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesma_pipeline.R
\name{bayesma_stan_code}
\alias{bayesma_stan_code}
\title{Generate Stan code for a bayesma specification}
\usage{
bayesma_stan_code(spec, format = TRUE)
}
\arguments{
\item{spec}{A \code{bayesma_spec} object.}

\item{format}{Logical. If \code{TRUE} (default), run the generated program
through Stan's \code{stanc --auto-format} for consistent indentation and
spacing. Falls back to the raw program if the formatter is unavailable.}
}
\value{
An object of class \code{"bayesma_stan_code"} -- a list with elements
\code{functions}, \code{data}, \code{transformed_data}, \code{parameters},
\code{transformed_parameters}, \code{model}, \code{generated_quantities}, and \code{full}.
}
\description{
Returns both the full Stan program and its decomposition into the standard
blocks (\code{functions}, \code{data}, \code{transformed_data}, \code{parameters},
\code{transformed_parameters}, \code{model}, \code{generated_quantities}). Missing blocks
are empty strings.
}
\details{
If \code{spec$custom_model} is non-NULL, the user's Stan code is returned
verbatim (and parsed into blocks for inspection).
}
